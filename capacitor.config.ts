import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Countries_Info',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
